useDynLib("rdict",
          .rdict_new = rdict_new,
          .rdict_put = rdict_put,
          .rdict_mput = rdict_mput,
          .rdict_get = rdict_get,
          .rdict_remove = rdict_remove,
          .rdict_stats = rdict_stats,
          .rdict_keys = rdict_keys,
          .rdict_count = rdict_count)

exportPattern("^[^\\.]")
